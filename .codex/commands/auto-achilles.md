---
description: Build a bounded autonomous execution brief for the next task.
argument-hint: [task or flags]
---

# auto-achilles

Inspect the repository context, then run:

```bash
node .tools/auto-achilles.mjs run --write --task "$ARGUMENTS"
```

Use the generated brief as the execution contract. Preserve existing project behavior, run focused verification, and report exact checks before claiming completion.
